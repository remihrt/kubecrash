apiVersion: v1
kind: Service
metadata:
  name: locust
  namespace: locust
  labels:
    app: locust
spec:
  type: NodePort
  selector:
    app: locust
  ports:
    - name: web
      port: 8089
      targetPort: web
      nodePort: 30089
